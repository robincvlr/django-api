apiVersion: v1
kind: Service
metadata:
    name: "azuredjangocluster-279f"
    labels:
        app: "azuredjangocluster-279f"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "azuredjangocluster-279f"